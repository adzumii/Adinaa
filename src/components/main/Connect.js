import Connect__container from "./Connect__container";

function Connect() {
    return (
        <div class="connect">
            < Connect__container />
        </div>
    );
}
export default Connect;