import Photo from "./Photo";

function Main__photos(){
    return (
        <div class="main__photos">
            < Photo />
            < Photo />
            < Photo />
            < Photo />
            < Photo />
            < Photo />
            < Photo />
            < Photo />
        </div>
    );
}
export default Main__photos;