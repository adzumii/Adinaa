import Contact__block from "./Contact__block";
import Input__block from "./Input__block";

function Connect__container() {
    return (
        <div class="connect__container">
            < Contact__block />
            < Input__block />
        </div>
    );
}
export default Connect__container;